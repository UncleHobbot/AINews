services:
  ainews:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./local-data:/app/data
      - ./local-keys:/app/keys
      - ./local-logs:/app/logs
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
